import React from "react";

const NotFound = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <p>
        For NotFound file : add 'not-found.tsx' file inside the app directory
      </p>
    </div>
  );
};

export default NotFound;
